#include <stdio.h>

/**
 * print_sign - prints if the sign of a number is + or -
 *
 * Return: 0 (Number is 0), -1 (-) or 1 (+)
 */

int print_sign(int n)
{
	if (n <= -1)
	{
	return (-1);
	}
	else
	{
	return (n >= 1)
	}
}	
