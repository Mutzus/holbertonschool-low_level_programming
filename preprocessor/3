#include <stdio.h>

#define ABS(x) ((x) < 0 ? -(x) : (x))

int main(void) {
    int num = -5;
    printf("Absolute value of %d is %d\n", num, ABS(num));

    double num_double = -7.5;
    printf("Absolute value of %.2f is %.2f\n", num_double, ABS(num_double));

    return 0;
}
